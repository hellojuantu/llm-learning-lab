# llm-learning-lab

Personal learning lab for LLM, deep learning, and from-scratch AI fundamentals.

## Projects

### micrograd

Path: `micrograd/`

Learning route:

```text
math review -> gradient practice -> Value usage -> Value implementation
-> TinyValue exercises -> Neuron/MLP -> training loop
```

Start here:

```text
micrograd/playground/README.md
```

## Run Jupyter

```bash
cd /Users/barry/IdeaProjects/llm
./start_notebook.sh
```

Then open:

```text
micrograd/playground/
```

## Notes

- `.venv/`, IDE files, caches, notebook checkpoints, model artifacts, and dataset folders are ignored.
- `micrograd/` is based on Andrej Karpathy's micrograd; `micrograd/playground/` contains the local learning course.
